# ── KG 파일 (미공개) ──
seed_kg_*.json
*.ttl
*.owl

# ── 노트북 체크포인트 ──
.ipynb_checkpoints/
**/.ipynb_checkpoints/

# ── Python 캐시 ──
__pycache__/
*.pyc
*.pyo

# ── 환경변수 / 인증 키 ──
.env
*credentials*.json
*service_account*.json
rugged-karma-*.json

# ── 노트북 (로컬 전용) ──
*.ipynb

# ── 작업 기록 (로컬 전용) ──
TASKS.md

# ── 임시 파일 ──
*.tmp
monitoring/cumulative/
docs/ontology_schema_v8.html
