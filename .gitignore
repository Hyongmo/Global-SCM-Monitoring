# ── 원본 수집 CSV (저작권 보호 — git 제외) ──
monitoring/*/gdelt_mon_daily_*.csv
monitoring/*/naver_mon_daily_*.csv

# ── KG 파일 (미공개) ──
seed_kg_*.json
*.ttl
*.owl

# ── 노트북 체크포인트 ──
.ipynb_checkpoints/
**/.ipynb_checkpoints/

# ── Python 캐시 ──
__pycache__/
*.pyc
*.pyo

# ── 환경변수 / 인증 키 ──
.env
*credentials*.json
*service_account*.json

# ── 임시 파일 ──
*.tmp
cumulative/
monitoring/cumulative/
