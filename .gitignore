# ── KG 파일 ──
# seed_kg_v4.json은 주간 파이프라인(GitHub Actions)에서 필요하므로 추적 대상
# seed_kg_*.json  ← 2026-08-23 해제 (weekly_pipeline.py step1 의존)
*.ttl
*.owl

# ── 노트북 체크포인트 ──
.ipynb_checkpoints/
**/.ipynb_checkpoints/

# ── Python 캐시 ──
__pycache__/
*.pyc
*.pyo

# ── 환경변수 / 인증 키 ──
.env
*credentials*.json
*service_account*.json
rugged-karma-*.json

# ── 노트북 (로컬 전용) ──
*.ipynb

# ── 작업 기록 (로컬 전용) ──
TASKS.md

# ── 임시 파일 ──
*.tmp
monitoring/cumulative/
docs/ontology_schema_v8.html

# ── 루트 HTML 뷰어 (docs/ 사본만 배포) ──
weekly_report.html
